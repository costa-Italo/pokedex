import styled from "styled-components"

export const PaginationsStyles = styled.div`
    display: flex;
    flex-direction: row;
    justify-content: center;
    align-items: center;
    gap: 2rem;

    background-color: black;

    height: 3rem;

    color: white;
    font-size: 1.25rem;

    cursor: pointer;
`